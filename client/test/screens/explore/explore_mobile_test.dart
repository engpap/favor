import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
//import 'package:project/colors.dart';
import 'package:project/screens/explore/providerExplore.dart';
//import 'package:project/search_bar.dart';
import 'package:provider/provider.dart';

void main() {
  /*group('SearchBar', () {
    testWidgets('should display a CupertinoTextField',
        (WidgetTester tester) async {
      await tester.pumpWidget(
        MultiProvider(
          providers: [
            ChangeNotifierProvider(create: (_) => ExploreQuery()),
          ],
          child: CupertinoApp(
            home: Scaffold(
              body: buildSearchBar(context),
            ),
          ),
        ),
      );

      expect(find.byType(CupertinoTextField), findsOneWidget);
    });

    testWidgets('should display a prefix button with a search icon',
        (WidgetTester tester) async {
      await tester.pumpWidget(
        MultiProvider(
          providers: [
            ChangeNotifierProvider(create: (_) => ExploreQuery()),
          ],
          child: CupertinoApp(
            home: Scaffold(
              body: buildSearchBar(context),
            ),
          ),
        ),
      );

      final prefixButton = find.byType(CupertinoButton).first;
      expect(prefixButton, findsOneWidget);
      expect(find.byIcon(CupertinoIcons.search), findsOneWidget);
    });
  });*/
}
